<h1>Editing category</h1>

<%= render "form", category: @category %>

<%= button_to "Discard this category", @category, method: :delete, class: "btn btn-danger", form: { data: { turbo_confirm: 'Are you sure?' } }  %>